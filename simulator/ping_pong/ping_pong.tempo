func@(A,B) pingPong(count: Int@[A,B]) Int@A {
  if count > 0 {
    let result: Int@B = await A->B count;
    pingPong(count - 1)@(B,A);
  }
}

func@(A,B) Start() {
  let x: async Int@A = pingPong(4)@(A,B);
}
